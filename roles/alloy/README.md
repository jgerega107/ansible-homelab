# Alloy role
Simple "wrapper" role that supplies a config to the official Grafana Alloy role.

# Alloy Remote Config Server
By default, the remote config server config is included and used. You can use https://github.com/jgerega107/alloy-remote-config-server as an example server.