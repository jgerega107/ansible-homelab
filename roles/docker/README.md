# Docker role

Simple role to install Docker Container Engine from scratch. Works with Ubuntu/Debian type systems only.

Includes functionality to backup the created Docker system directories. Requires a `docker_backup_password` variable set.
