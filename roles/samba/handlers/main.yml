---
- name: Reload NFS exports
  ansible.builtin.command:
      cmd: exportfs -arv
  changed_when: true

- name: Restart Samba
  ansible.builtin.service:
      name: "{{ samba_service_name }}"
      state: restarted
